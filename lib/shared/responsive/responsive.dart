/// Shared responsive foundation: window size classes, adaptive helpers, and
/// the content-width wrapper used to keep list/detail surfaces readable on
/// tablets and desktop windows. Native Flutter primitives only — no external
/// responsive package (see the responsive audit note in docs/).
library;

export 'responsive_breakpoints.dart';
export 'responsive_content.dart';
